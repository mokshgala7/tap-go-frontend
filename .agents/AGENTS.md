# Workspace Agent Guidelines & Rules

- **Automatic Git Sync**: Whenever any code, configuration, or documentation files are created, modified, or deleted, automatically run `git add .`, `git commit -m "<descriptive message>"`, and `git push origin main` to ensure GitHub, Railway, and Vercel are always in sync with the latest code.
